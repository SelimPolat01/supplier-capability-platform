@using System.Security.Claims
@using SupplierCapabilitiesAndManagementSystem.Entities
@using SupplierCapabilitiesAndManagementSystem.Models
@using SupplierCapabilitiesAndManagementSystem.Models.DTO

@{
    int userId = int.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier)!);

    SearchBarModel searchBarModel = new()
    {
        Name = "SearchString",
        Value = ViewBag.CurrentSearch as string ?? "",
        ButtonType = "submit",
        AspController = "Supplier",
        AspAction = "Machines",
        Placeholder = "Search machines...."
    };
}

<div class="all-users-container">
    <div class="users-flex-container">
        <h2 class="all-users-text">
            <i class="fa-solid fa-gears" style="margin-right: 8px; color: #cbd5e1;"></i> All Machines
        </h2>        
        <partial name="_SearchBar" model="searchBarModel"></partial>
        </div>
        <vc:supplier-all-machines-with-pagination user-id="@userId" page-number="@ViewBag.CurrentPage" sort-by="@ViewBag.CurrentSort" search-string="@ViewBag.CurrentSearch"></vc:supplier-all-machines-with-pagination>
    </div>

    @section SearchBarStyles {
        <link rel="stylesheet" href="~/css/SearchBarStyles.css" />
    }
